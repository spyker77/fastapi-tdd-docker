build:
  docker:
    web: Dockerfile
run:
  web: gunicorn -b 0.0.0.0:$PORT -k uvicorn.workers.UvicornWorker app.main:app
  worker:
    command:
      - celery worker -A app.background.worker
    image: web
