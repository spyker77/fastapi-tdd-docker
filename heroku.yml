build:
  docker:
    web: Dockerfile.prod
run:
  web: gunicorn -b 0.0.0.0 -k uvicorn.workers.UvicornWorker app.main:app
  worker:
    command:
      - celery worker -A app.background.worker -l INFO
    image: web
